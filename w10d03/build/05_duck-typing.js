"use strict";
const login = (user) => {
    user.username;
    return true;
};
const myObj = {
    username: 'jstamos',
    password: '1234',
    bffs: [],
    tvShows: [],
    bands: []
};
login(myObj);
